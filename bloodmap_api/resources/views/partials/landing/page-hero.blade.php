<section class="hm-page-hero">
    <div class="hm-page-hero-bg hm-breathe"></div>
    <div class="hm-page-hero-content hm-reveal">
        <p class="hm-eyebrow">{{ $eyebrow }}</p>
        <h1 class="hm-page-title hm-glow">{{ $title }}</h1>
        @if (!empty($subtitle))
            <p class="hm-page-subtitle">{{ $subtitle }}</p>
        @endif
        <div class="hm-artery-line"></div>
    </div>
</section>
